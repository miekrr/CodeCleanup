
if((something == -1) && (somethingelse == -1) && (etc == -1)) {
    // ... 1
}

if((something == -1) && (somethingelse == -1) ) {
    // ... 2
} 

if((something == -1) && 
	((somethingelse == -1) &&
	 (etc == -1)) {
    // ... 3
}

if((something == -1) && 
	((somethingelse == -1) &&  (test == 1) &&
	 (etc == -1)) {
    // ... 4
}


if( (something == -1) && 
	somethingelse == -1 && 
	(etc == -1)) {
    // ... 5
}

case juju:
 // yeye
break;

case somethingelse:
	i++;
break;

case jajajjaja:

case uncase:
case somethingelse:
case juju:
	i++;
break;
